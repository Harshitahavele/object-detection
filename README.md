# object-detection
INFO:-
a machine learning / deep learning project for object detection .
this project aim to detect the objects like person, any item etc. creating the green rectangular boxes around it and recognized it.
this projects require tensorflow,open cv and kerad ,cvlib etc.

WORKING:-
in this project we make the fronted and the object detection coding part further we will updates on this to create the backend part ,
the user will open through the web app after it open the camera then click on the "generate sentence" in this the fronted scaned image sent to backend part then it will process it.
after that backend processing the setence of recognising object is seen on the fronted part.
this is how we create it

UPDATES:-
we will on the direction og making the  backend part ,we just create the fronted and the code of the machine.
